{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "installCommand": "yarn install --frozen-lockfile && rm -rf node_modules/cypress",
  "buildCommand": "yarn install --frozen-lockfile && yarn build"
}
